get:
  summary: Get all users
  responses:
    "200":
      description: A list of users
      content:
        application/json:
          schema:
            $ref: "../schemas/user.yaml#/GetUsersResponse"
  tags:
    - Users
