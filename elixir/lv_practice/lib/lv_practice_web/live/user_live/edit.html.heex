<%= live_component LvPracticeWeb.UserLive.FormComponent,
    id: @user.id,
    title: @page_title,
    action: :edit,
    user: @user,
    return_to: Routes.live_path(@socket, LvPracticeWeb.UserLive.Show, @user.id) %>
